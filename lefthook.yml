pre-commit:
  jobs:
    - name: JavaScript
      glob: '*.{js,mjs,tsx,ts}'
      run: pnpm oxlint {staged_files}
    - name: Other files
      glob: '*.{js,mjs,cjs,tsx,ts,hmtl,json,yaml,css}'
      run: pnpm oxfmt --check --no-error-on-unmatched-pattern
